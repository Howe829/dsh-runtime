# One dual-face Loader row. The package's default export mounts the Host
# gateway; its dsh.client declaration exposes the Web Runtime Explorer.
- insert:
    - id: dsh-runtime
      name: @howardchan/dsh-runtime
